# SAL dependencies, grouped so `setup.py` can offer them as extras.
#
# `pip install -r requirements.txt` installs EVERYTHING, which is what it has
# always done. `pip install -e .` installs the [core] group only; the rest are
# extras, because the generative stack below is large and version-fragile and is
# not imported by any file in this repository.
#
# The section markers are load-bearing: setup.py partitions on them.

# --- core ---
# Needed by src/ and tools/. Everything here is imported by tracked code.
numpy>=1.21.0
opencv-python>=4.5.0
pyyaml>=5.4.0
pillow>=9.0.0
tqdm>=4.62.0
psutil>=5.8.0
scipy>=1.9.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
pandas>=1.3.0
pytest>=7.0.0
# Trajectory metrics (ATE/RPE) and the TUM/KITTI/EuRoC trajectory readers.
evo>=1.23.0
# Per-process GPU telemetry: src/runtime_stress/telemetry.py imports `pynvml`,
# which this package provides. Without it the runtime-stress traces silently
# lose their VRAM and SM columns, because the import is guarded: a run completes
# and logs a warning rather than failing.
nvidia-ml-py>=12.0.0

# --- torch ---
# Depth-based perturbations and the SLAM wrappers that stage tensors host-side.
# Install the build matching your CUDA from https://pytorch.org, then this.
torch>=2.0.0
torchvision>=0.15.0

# --- daynight ---
# ONLY for the day-to-night perturbation, which runs the vendored img2img-turbo
# model in deps/perturbations/. No file in src/ or tools/ imports any of these;
# they are the submodule's own requirements. Large, and xformers in particular
# pins torch tightly, so they are kept out of the default install.
diffusers>=0.24.0
transformers>=4.30.0
accelerate>=0.20.0
xformers>=0.0.22
peft>=0.10.0
lpips>=0.1.4
clean-fid>=0.1.35
einops>=0.6.1
open-clip-torch>=2.20.0
timm>=0.9.2
dominate>=2.9.0
